# Claude API key. Optional: everything except the AI features works without it.
# You can also set this from the in-app Settings screen instead.
ANTHROPIC_API_KEY=

# Where the SQLite database and uploaded files live (default: ./data)
# EVERNOTION_DATA_DIR=./data
